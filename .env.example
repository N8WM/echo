# App
TOKEN=your_discord_bot_token_here
DEV_GUILD_IDS=guild_id_1,guild_id_2

# Discord Intents (comma-separated, see GatewayIntentBits)
# Leave empty to use defaults (Guilds,GuildMembers,GuildMessages,GuildMessageReactions,GuildModeration,MessageContent)
DISCORD_INTENTS=

# Prisma
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# LLM / Vector Search
# OLLAMA_HOST=http://host.docker.internal:11434

# Optional Features

# Smart Command Deployment - Only update commands that have changed (default: false)
# USE_SMART_COMMAND_DEPLOYMENT=true

# Health Check Server - Enable HTTP health/ready endpoints (default: false)
# ENABLE_HEALTH_CHECK=true
# HEALTH_CHECK_PORT=3000
